import { ScrollAnimate } from './scroll-animate';

describe('ScrollAnimate', () => {
  it('should create an instance', () => {
    const directive = new ScrollAnimate();
    expect(directive).toBeTruthy();
  });
});
